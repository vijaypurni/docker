sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t vijaypurni/docker-react -f dockerfile.dev .

script:
  - docker run -e CI=true vijaypurni/docker-react npm run test

